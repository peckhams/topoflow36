# 2025-09-28.  S.D. Peckham
# Requirements of the topoflow36 Python package.
# This has no support for nb_conda or jupyterlab.
# Created for NGIAB Docker image that uses Python 3.9.
# NextGen's ngen and t-route need numpy>=1,<2.
# Commented out GDAL line.  Install instead with python3-gdal
#   (system level, not venv) and then edit the PYTHONPATH.
# Lowered setuptools version for t-route compatibility.
#-----------------------------------------------------------------
# In the modified NGIAB Docker image, system-level dependencies
# of matplotlib were installed with python3-matplotlib via dnf.
# This seems to install matplotlib version 3.4.3.
# Some of the matplotlib package dependencies are:
#   fonttools, pillow, numpy, pyparsing, python-dateutil, six,
#   and tornado.
# Docker build output shows:
#   python3-kiwisolver-1.4.1, python3-pyparsing 2.4.7,
#   python3-pillow-10.0.1, python3-matplotlib-3.4.3,
#   python3-cycler-0.11.0
#
# The versions of these dependencies were lowered as shown to
# ensure compatibility with matplotlib version 3.4.3.
# Raised min version of numpy for matplotlib.
# Max version of numpy for matplotlib 3.4.3 may be 1.20.3.
#
# May need to comment out the matplotlib line as done with GDAL.
#-----------------------------------------------------------------
Brotli==1.0.9
certifi==2025.8.3
cftime==1.6.4
cfunits==3.3.7
contourpy==1.2.1
cycler==0.11.0
fonttools==4.29.1
## fonttools==4.55.3
## GDAL==3.4.3
imageio==2.37.0
imageio-ffmpeg==0.6.0
importlib_resources==6.4.0
kiwisolver==1.4.4
matplotlib==3.4.3
## matplotlib==3.9.2
netCDF4==1.6.5
numpy>=1.15,<2
packaging==25.0
pillow==9.0
## pillow==11.3.0
pip==26.0
pyparsing==2.4.7
## pyparsing==3.2.0
python-dateutil==2.8.1
## python-dateutil==2.9.0.post0
scipy==1.13.1
setuptools>=64.0,<69.0
## setuptools==72.1.0
six==1.16.0
## six==1.17.0
tornado==6.1
## tornado==6.5.1
unicodedata2==15.1.0
wheel==0.45.1
zipp==3.21.0
